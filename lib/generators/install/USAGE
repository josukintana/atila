Description:
    Provides your existing project with social capabilities. You can choice to install all available functionalities, skip any of them or just to install those modules you prefer. 

    It assumes you already have a User model.

Example:
    'rails generate atila:install --with-all'

    'rails generate atila:install --help'
